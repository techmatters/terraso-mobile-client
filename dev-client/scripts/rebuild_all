#!/bin/bash

# Usage: rebuild_all [--run-only] [--debug-logs] <ios|android>

RUN_ONLY=false
DEBUG_LOGS=""
while [[ "$1" == --* ]]; do
    case "$1" in
        --run-only) RUN_ONLY=true; shift ;;
        --debug-logs) DEBUG_LOGS="--debug"; shift ;;
        *) echo "Unknown option: $1"; exit 1 ;;
    esac
done

PLATFORM="$1"

# Validate argument
if [[ "$PLATFORM" != "ios" && "$PLATFORM" != "android" ]]; then
    echo "Usage: rebuild_all [--run-only] [--debug-logs] <ios|android>"
    exit 1
fi

# Ensure we're in dev-client directory
if [[ "$PWD" == */dev-client ]]; then
    :
elif [[ -d "dev-client" ]]; then
    cd dev-client
else
    GIT_ROOT=$(git rev-parse --show-toplevel 2>/dev/null)
    if [[ -n "$GIT_ROOT" ]] && [[ -d "$GIT_ROOT/dev-client" ]]; then
        cd "$GIT_ROOT/dev-client"
    else
        echo "Error: Cannot find dev-client directory"
        exit 1
    fi
fi

if [[ "$RUN_ONLY" == false ]]; then
    echo "Building from $PWD for: $PLATFORM"

    rm -rf node_modules ios android

    # Clear Gradle build cache (can cause stale artifact issues)
    if [[ "$PLATFORM" == "android" ]]; then
        echo "Clearing Gradle build cache..."
        rm -rf ~/.gradle/caches/build-cache-1
    fi

    npm install
    npm run prepare
    npm run prebuild
else
    echo "Running from $PWD for: $PLATFORM (skipping rebuild)"
fi

# Filter noisy iOS log messages (handles multi-line messages, see script for patterns).
# No pseudo-TTY wrapper — the filter uses block suppression to catch continuation lines
# from messages that the log streamer splits when piped.
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
IOS_LOG_FILTER="node $SCRIPT_DIR/filter-ios-logs.js $DEBUG_LOGS"

if [[ "$PLATFORM" == "ios" ]]; then
    if [[ -n "$IOS_DEVICE_ID" ]]; then
        npm run ios -- --device "$IOS_DEVICE_ID" 2>&1 | $IOS_LOG_FILTER
    else
        npm run ios 2>&1 | $IOS_LOG_FILTER
    fi
else
    npm run android
fi
