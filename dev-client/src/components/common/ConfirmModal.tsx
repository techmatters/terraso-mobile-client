type Props = {
  title: string;
  bodyParagraphs: string[];
  actionName: string;
};

/**
 * Modal presented to a user when asked to confirm a decision
 */
const ConfirmModal = ({title, bodyParagraphs, actionName}: Props) => {
  return <Modal></Modal>;
};
