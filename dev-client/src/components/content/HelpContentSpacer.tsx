import {StyleSheet, View} from 'react-native';

export const HelpContentSpacer = () => {
  return <View style={styles.spacer} />;
};

const styles = StyleSheet.create({
  spacer: {
    marginLeft: 6,
  },
});
